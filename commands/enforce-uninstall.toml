description = "Remove all enforce-mode artifacts (flag files, config, statusline)"
prompt = """Run the enforce-mode cleanup script:
```
node ${CLAUDE_PLUGIN_ROOT}/hooks/enforce-uninstall.js
```

Report what was removed."""
